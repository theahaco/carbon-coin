node_modules/
dist/
.env
.deployment.json
.devnet.json
*.log
.DS_Store
web/.astro/

# Pinned, reproducibly built aha SDK (see prototype.json).
.prototype/

# Disposable local CLI walkthrough (encrypted keys and public results).
.demo/
